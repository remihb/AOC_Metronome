package ihm;

import command.Command;

/**
 * @author remihb
 * Button for decrement the measure value of the engine
 */
public class IncMeasureButton extends Button{

	public IncMeasureButton(Command command) {
		super(command);
	}
}