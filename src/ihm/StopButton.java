package ihm;

import command.Command;

/**
 * @author remihb
 * Button for stopping the engine
 */
public class StopButton extends Button {

	public StopButton(Command command) {
		super(command);
	}
}
