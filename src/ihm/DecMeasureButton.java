package ihm;

import command.Command;

/**
 * @author remihb
 * Button for decrement the measure value of the engine
 */
public class DecMeasureButton extends Button {

	public DecMeasureButton(Command command) {
		super(command);
	}

}
