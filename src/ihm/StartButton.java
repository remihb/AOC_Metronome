package ihm;

import command.Command;

/**
 * @author remihb
 * Button for stopping the engine
 */
public class StartButton extends Button {

	public StartButton(Command command) {
		super(command);
	}
}
